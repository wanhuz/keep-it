
<div class="card note" style="width: 18rem;" data-last-updated="{{ $updated_at }}" data-id="{{ $id }}" >
    <div class="card-body text-start">
        <h5 class="card-title">{{ $title }}</h5>
        <p class="card-text">{{ $body }}</p>
    </div>
</div>
